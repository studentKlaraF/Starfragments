@model IEnumerable<SeminarskaNaloga.Models.Racun>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.datum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.kolicina)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.cena)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.postnina)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Uporabnik)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Narocilo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.datum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.kolicina)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cena)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.postnina)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Uporabnik.UporabnikId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Narocilo.NarociloId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RacunId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RacunId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RacunId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
